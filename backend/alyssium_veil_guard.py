import random
import math

def module_0(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 20

def module_1(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 30

def module_2(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 43

def module_3(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 46

def module_4(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 26

def module_5(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 41

def module_6(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 50

def module_7(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 45

def module_8(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 20

def module_9(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 35

def module_10(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 46

def module_11(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 37

def module_12(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 22

def module_13(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 18

def module_14(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 13

def module_15(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 24

def module_16(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 32

def module_17(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 45

def module_18(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 40

def module_19(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 23

def module_20(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 31

def module_21(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 12

def module_22(x):
    if x < 0:
        return math.exp(x)
    elif x % 2 == 0:
        return x ** 2 + random.randint(1, 10)
    else:
        return x + 42